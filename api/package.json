{
  "name": "homepulse-api",
  "version": "1.0.0",
  "description": "HomePulse Azure Functions Backend (v4 Programming Model)",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@azure/functions": "^4.6.0",
    "@azure/cosmos": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  }
}
